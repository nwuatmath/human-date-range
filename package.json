{
  "name": "human-date-range-nwuatmath",
  "version": "1.0.0",
  "description": "human date range",
  "main": "dist/index.js",
  "module": "src/index.js",
   "scripts": {
    "test": "jest --coverage",
    "build": "rollup -c --environment format:cjs && rollup -c --environment format:es",
    "prepublish": "npm test && npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-jest": "^20.0.3",
    "babel-preset-es2015": "^6.24.1",
    "buble": "^0.15.2",
    "jest": "^20.0.4",
    "rollup": "^0.41.6",
    "rollup-plugin-buble": "^0.15.0"
  }
}
